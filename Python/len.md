# len函数
### 在Python中，len() 是一个内置函数，用于返回对象的“长度”（即元素个数)。它适用于多种内置数据类型。
### 基础语法
```python
len(object)
```
### 常见实例：
返回字符个数：
```python
s = "Hello, world!"
print(len(s))  # 输出: 13，空格也算一个占位符
```
## 切记：len函数不能返回负数，只能返回正数或者0
